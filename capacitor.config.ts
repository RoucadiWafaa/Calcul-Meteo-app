import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'calcul_et_meteo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
